variables:
  MAVEN_OPTS: "-Dmaven.deploy.releases=$MAVEN_DEPLOY_RELEASES -Dmaven.deploy.snapshots=$MAVEN_DEPLOY_SNAPSHOTS -Dmaven.repo.local=.m2/repository"

default:
  image: maven:3.8.4-jdk-8

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - mvn -s .mvn/settings.xml deploy
